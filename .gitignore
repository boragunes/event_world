# --- Source paper PDFs (copyrighted, large): kept local, not committed. ---
/papers/

# --- Raw datasets (large): never committed. Fetch via datasets/*/download_*.sh ---
/data/
*.bag
*.bag.bz2
*.h5
*.hdf5
*.raw
*.zip
*.bz2

# --- evo raw result archives (keep metrics.json + plots instead) ---
*.zip

# --- ESVIO/ESIO/SDEVO run intermediates (keep stamped_traj.tum + metrics.json + plots + node logs) ---
play.log
roscore.log
record.log
evo_traj.log
timer.log
poses_tum_format.txt
poses_interpolated.txt
*_odometry.tum
esvio_result_*.csv
esvio_result_*.txt

# --- Python / virtualenv ---
.venv/
venv/
__pycache__/
*.pyc

# --- Harness / editor / OS local files ---
.claude/
.vscode/
.idea/
.DS_Store
*.swp

# DEVO model weights (get from shadymeowy/dpvo-clean weights/devo.pth)
devo/weights/*.pth
**/__pycache__/
