<launch>
    <!-- Headless variant of upstream esvio_estimator/launch/esvio_VECtor_small_scale.launch.
         IDENTICAL nodes/params, with ONLY the GUI cosmetics removed so it runs without a
         display in a container:
           * stereo_image_tracker: dropped launch-prefix="gnome-terminal -x" (run as a plain node)
           * removed the rviz visualisation node
         No algorithm or config change. config_path/esvio_path default to the in-image VECtor
         small-scale config and can be overridden to run other datasets. -->

    <arg name="config_path" default = "$(find feature_tracker)/../config/esvio_VECtor_small_scale/esvio.yaml" />
    <arg name="esvio_path"  default = "$(find feature_tracker)/../config/esvio_VECtor_small_scale/" />

    <!-- stereo event feature -->
    <node name="stereo_event_tracker" pkg="feature_tracker" type="stereo_event_tracker" output="screen" respawn="true">
        <param name="config_file" type="string" value="$(arg config_path)" />
        <param name="esvio_folder" type="string" value="$(arg esvio_path)" />
    </node>

    <!-- stereo image feature -->
    <node name="stereo_image_tracker" pkg="feature_tracker" type="stereo_image_tracker" output="screen">
        <param name="config_file" type="string" value="$(arg config_path)" />
        <param name="esvio_folder" type="string" value="$(arg esvio_path)" />
    </node>

    <!-- esvio estimator -->
    <node name="stereo_esvio_estimator" pkg="esvio_estimator" type="stereo_esvio_estimator" output="screen" respawn="true">
       <param name="use_stereo_correction" type="bool" value="true"/>
       <param name="opt_verbose" type="bool" value="false"/>
       <param name="config_file" type="string" value="$(arg config_path)" />
       <param name="esvio_folder" type="string" value="$(arg esvio_path)" />
    </node>

    <!-- pose graph -->
    <node name="pose_graph" pkg="pose_graph" type="pose_graph" output="screen" respawn="true">
        <param name="config_file" type="string" value="$(arg config_path)" />
        <param name="visualization_shift_x" type="int" value="0" />
        <param name="visualization_shift_y" type="int" value="0" />
        <param name="skip_cnt" type="int" value="0" />
        <param name="skip_dis" type="double" value="0" />
    </node>

</launch>
